# caddy server

open source web server written in Go

installation > run provided commands (better to remove apache or nginx before !)
check if running with systemctl
caddy.danb.me ?!

A/AAAA DNS records ?!

follow instructions to serve your own site !

Caddyfile (in project ?!)
